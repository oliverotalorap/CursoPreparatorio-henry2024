console.log( 0.5+0.5 + " -Suma de strings");
var hijo = "NICO";
var hija = "CELESTE";
var saludo1 = " HELLO " + hijo;
var saludo2 = "BUENOS DIAS";
// console.log(saludo);
console.log(saludo1);
console.log(saludo1+" "+ hija+" "+hijo+" "+ saludo2 );


console.log( 1+1 + " if and else");
puedeManejar(19);
puedeManejar(17);
function puedeManejar(edad) {
    if (edad >= 18) {console.log(true) }
    else {console.log(false);};
};

console.log( 2+1 + " -titulo");

// function devolverString(string)
    // Debe retornar un string.
    // Tu código:
    var devolverString = "string";
    var saludo = "hola";
    console.log(devolverString);
    // return palabra; -NO FUNCIONA
    
    // function suma(x, y) {
        // Retorna el resultado de su suma.
        // Tu código:
    var uno = "x";
    var dos = "y";
    console.log(uno + dos);
    // return uno + dos; -NO FUNCIONA 
    //}
     
    // function resta(x , y) {
        // Retorna el resultado de la resta.
        // Tu código:
    var uno = "x";
    var dos = "y";
    console.log(uno - dos);
    // }
     
    // function divide(x, y) {
        // Retorna el resultado de su división.
        // Tu código:
    var uno = 1;
    var dos = 2;
    console.log(uno / dos);
    // }
     
    // function multiplica(x, y) {
        // Retorna el resultado de su multiplicación.
        // Tu código:
    var uno = "x";
    var dos = "y";
    console.log(uno * dos);
    // }
     
    // function obtenerResto(x, y) {
        // Obten el resto de la división de "x" entre "y".
        // Tu código:
    var uno = "3";
    var dos = "2";
    console.log(uno % dos);  
    // }
    
