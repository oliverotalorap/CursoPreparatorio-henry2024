/*3️⃣ EJERCICIO 03 3️⃣*/

// function sonIguales(x, y) {
    // Retorna true si "x" e "y" son iguales.
    // De lo contrario, retorna false.
    console.log(1+' Ejer');
    function sonIguales(x, y) {
    if (x == y) { console.log(true); } 
    else { console.log(false); } } 
    sonIguales(2+1, 3);

 // function tienenMismaLongitud(str1, str2) {
    // Retorna true si los dos strings tienen la misma longitud.
    // De lo contrario, retorna false.
    // Tu código:
    console.log(2 + ' Ejer');
    function longitud(str1, str2) {
   if (str1.length === str2.length) console.log(true); 
   if (str1 !== str2) console.log(false);
   else { console.log(false); } 
} 
   // longitud(2+1, 4-1);
   longitud("caja", "casa");

  // function menosQueNoventa(num) {
    // Retorna true si el argumento "num" es menor que noventa.
    // De lo contrario, retorna false.
    // Tu código:
    console.log(3+' Ejer');
      function menosDe90(num) {
      if (num <= 90) { console.log(true); } 
      else { console.log(false); } } 
      menosDe90(89);

 // function mayorQueCincuenta(num) {
    // Retorna true si el argumento "num" es mayor que cincuenta.
    // De lo contrario, retorna false.
    // Tu código:
    console.log(3 + 1);
    function mayorDe50(num) {
      if (num >= 50) { console.log(true);} 
      else { console.log(false);}} 
      mayorDe50(49);
 
 // function esPar(num) {
    // Retorna true si "num" es par.
    // De lo contrario, retorna false.
    // Tu código:
    console.log(4 + 1);
    function espar(num) {
    if (num % 2 === 0) { console.log(true);} 
    else { console.log(false);}}
    espar(21);

 // function esImpar(num) {
    // Retorna true si "num" es impar.
    // De lo contrario, retorna false.
    // Tu código:
    console.log(5 + 1);
    function esImpar(num) {
    if (num % 2 != 0) { console.log(true);} 
    else { console.log(false);}}
    esImpar(11);
